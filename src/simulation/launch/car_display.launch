<launch>

    <!-- 设置参数 -->
    <param name="robot_description" textfile="$(find simulation)/urdf/car.urdf" />

    <!-- 启动 rviz -->
    <node pkg="rviz" type="rviz" name="rviz" args="-d $(find simulation)/config/show_car.rviz" />

</launch>